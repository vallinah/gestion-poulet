<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
                             https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0"
         metadata-complete="true">
    <!-- Configuration du servlet pour Book -->
    <servlet>
        <servlet-name>BookServlet</servlet-name>
        <servlet-class>hery.projet.servlet.controller.BookServlet</servlet-class>
    </servlet>

    <!-- Mappage du servlet pour Book -->
    <servlet-mapping>
        <servlet-name>BookServlet</servlet-name>
        <url-pattern>/books</url-pattern>
    </servlet-mapping>

    <!-- Configuration du servlet pour Author -->
    <servlet>
        <servlet-name>AuthorServlet</servlet-name>
        <servlet-class>hery.projet.servlet.controller.AuthorServlet</servlet-class>
    </servlet>

    <!-- Mappage du servlet pour Author -->
    <servlet-mapping>
        <servlet-name>AuthorServlet</servlet-name>
        <url-pattern>/authors</url-pattern>
    </servlet-mapping>

    <!-- Configuration du servlet pour Category -->
    <servlet>
        <servlet-name>CategoryServlet</servlet-name>
        <servlet-class>hery.projet.servlet.controller.CategoryServlet</servlet-class>
    </servlet>

    <!-- Mappage du servlet pour Category -->
    <servlet-mapping>
        <servlet-name>CategoryServlet</servlet-name>
        <url-pattern>/categories</url-pattern>
    </servlet-mapping>

    <!-- Configuration du servlet pour Loan -->
    <servlet>
        <servlet-name>LoanServlet</servlet-name>
        <servlet-class>hery.projet.servlet.controller.LoanServlet</servlet-class>
    </servlet>

    <!-- Mappage du servlet pour Loan -->
    <servlet-mapping>
        <servlet-name>LoanServlet</servlet-name>
        <url-pattern>/loans</url-pattern>
    </servlet-mapping>

    <!-- Configuration du servlet  -->
    <servlet>
        <servlet-name>FormAuthorServlet</servlet-name>
        <servlet-class>hery.projet.servlet.controller.FormAuthorServlet</servlet-class>
    </servlet>

    <!-- Mappage du servlet -->
    <servlet-mapping>
        <servlet-name>FormAuthorServlet</servlet-name>
        <url-pattern>/form-author</url-pattern>
    </servlet-mapping>

    <!-- Configuration du servlet  -->
    <servlet>
        <servlet-name>FormBookServlet</servlet-name>
        <servlet-class>hery.projet.servlet.controller.FormBookServlet</servlet-class>
    </servlet>

    <!-- Mappage du servlet -->
    <servlet-mapping>
        <servlet-name>FormBookServlet</servlet-name>
        <url-pattern>/form-book</url-pattern>
    </servlet-mapping>

    <!-- Configuration du servlet  -->
    <servlet>
        <servlet-name>FormCategoryServlet</servlet-name>
        <servlet-class>hery.projet.servlet.controller.FormCategoryServlet</servlet-class>
    </servlet>

    <!-- Mappage du servlet -->
    <servlet-mapping>
        <servlet-name>FormCategoryServlet</servlet-name>
        <url-pattern>/form-category</url-pattern>
    </servlet-mapping>

    <!-- Configuration du servlet  -->
    <servlet>
        <servlet-name>FormLoanServlet</servlet-name>
        <servlet-class>hery.projet.servlet.controller.FormLoanServlet</servlet-class>
    </servlet>

    <!-- Mappage du servlet -->
    <servlet-mapping>
        <servlet-name>FormLoanServlet</servlet-name>
        <url-pattern>/form-loan</url-pattern>
    </servlet-mapping>

    <!-- Configuration du servlet  -->
    <servlet>
        <servlet-name>FormPublisherServlet</servlet-name>
        <servlet-class>hery.projet.servlet.controller.FormPublisherServlet</servlet-class>
    </servlet>

    <!-- Mappage du servlet -->
    <servlet-mapping>
        <servlet-name>FormPublisherServlet</servlet-name>
        <url-pattern>/form-publisher</url-pattern>
    </servlet-mapping>

    <!-- Configuration du servlet  -->
    <servlet>
        <servlet-name>LoginServlet</servlet-name>
        <servlet-class>hery.projet.servlet.controller.LoginServlet</servlet-class>
    </servlet>

    <!-- Mappage du servlet -->
    <servlet-mapping>
        <servlet-name>LoginServlet</servlet-name>
        <url-pattern>/login</url-pattern>
    </servlet-mapping>

    <!-- Configuration du servlet  -->
    <servlet>
        <servlet-name>FormLoginServlet</servlet-name>
        <servlet-class>hery.projet.servlet.controller.FormLoginServlet</servlet-class>
    </servlet>

    <!-- Mappage du servlet -->
    <servlet-mapping>
        <servlet-name>FormLoginServlet</servlet-name>
        <url-pattern>/form-login</url-pattern>
    </servlet-mapping>

    <!-- Configuration du servlet  -->
    <servlet>
        <servlet-name>BookDetailsServlet</servlet-name>
        <servlet-class>hery.projet.servlet.controller.BookDetailsServlet</servlet-class>
    </servlet>

    <!-- Mappage du servlet -->
    <servlet-mapping>
        <servlet-name>BookDetailsServlet</servlet-name>
        <url-pattern>/book-details</url-pattern>
    </servlet-mapping>

    <!-- Configuration du servlet  -->
    <servlet>
        <servlet-name>PublisherServlet</servlet-name>
        <servlet-class>hery.projet.servlet.controller.PublisherServlet</servlet-class>
    </servlet>

    <!-- Mappage du servlet -->
    <servlet-mapping>
        <servlet-name>PublisherServlet</servlet-name>
        <url-pattern>/publishers</url-pattern>
    </servlet-mapping>

    <!-- Configuration du servlet  -->
    <servlet>
        <servlet-name>DashboardServlet</servlet-name>
        <servlet-class>hery.projet.servlet.controller.DashboardServlet</servlet-class>
    </servlet>

    <!-- Mappage du servlet -->
    <servlet-mapping>
        <servlet-name>DashboardServlet</servlet-name>
        <url-pattern>/dashboards</url-pattern>
    </servlet-mapping>
</web-app>
