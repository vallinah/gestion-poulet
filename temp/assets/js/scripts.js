// scripts.js

// Exemple: Initialisation des icônes Feather
document.addEventListener("DOMContentLoaded", function () {
    if (typeof feather !== 'undefined') {
        feather.replace();
    }
});
